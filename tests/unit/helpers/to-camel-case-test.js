import { toCamelCase } from 'hello/helpers/to-camel-case';
import { module, test } from 'qunit';

module('Unit | Helper | to camel case');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toCamelCase([42]);
  assert.ok(result);
});
